<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Rectangle Checker</title>
  </head>
  <body>
    <h1>Is it a square?</h1>

    <h5>Your rectangle's length is @Model["ResultingRectangle"].GetLength()</h5>
    <h5>Your rectangle's width is @Model["ResultingRectangle"].GetWidth()</h5>

    @if (@Model["ResultingRectangle"].IsSquare()) {
      <h3>Your rectangle is a square!</h3>
      <h5>Surface area of a cube with those dimensions: @Model["ResultingCube"].GetSurfaceArea()</h5>
      <h5>Volume of a cube with those dimensions: @Model["ResultingCube"].GetVolume()</h5>
    } else {
      <h3>Your rectangle isn't a square!</h3>
    }

    <p><a href="/">Make another rectangle!</a></p>
  </body>
</html>
